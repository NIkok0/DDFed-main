# =============================================================================
# ddfed_fl — 联邦学习（FedAvg + DDFed / TMCFE / Lepcat）依赖
# =============================================================================
# 使用方式（在 DDFed-main 仓库根目录或已将根目录加入 PYTHONPATH 时）：
#   pip install -r ddfed_fl/requirements.txt
#
# GPU 版 PyTorch 请按官方说明单独安装后再装其余条目，例如：
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 与 sibling 目录的关系：
# - ddfed_crypto：安全聚合原语（本目录 train 脚本会 import），需 gmpy2；详见 ../ddfed_crypto/requirements.txt
# - ddfed_fu：联邦遗忘（FedEraser），与本文件无关
# =============================================================================

# --- 深度学习栈（CPU 默认可从 PyPI 解析；与 torchvision 版本需相互兼容）---
torch>=2.0.0
torchvision>=0.15.0

# --- 数值、表格与可视化（train_fedavg_*.py、fed_utils 等）---
numpy>=1.22.0
pandas>=1.3.0
matplotlib>=3.5.0

# --- 数据增强 / 科学计算 ---
# fed_utils 使用 scipy.ndimage.interpolation.rotate，该路径在 scipy 2.0 已移除，故上限 <2
scipy>=1.7.0,<2.0.0
tqdm>=4.62.0
Pillow>=9.0.0

# --- 安全聚合（ddfed_crypto：Rodot+ / TMCFE / DMCFE-IP）---
gmpy2>=2.1.0

# --- 可选：DDMCFE 等使用真实双线性配对（未安装时用 ddfed_crypto 内 pairing_mock）---
# charm-crypto>=0.43
